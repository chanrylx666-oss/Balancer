# 双平面动平衡仿真程序：实施计划

技术基线：创建 .NET 10 WPF 解决方案。算法使用 `System.Numerics.Complex`，不引入算法依赖；使用 xUnit 做自动化验证。WPF 使用 CommunityToolkit.Mvvm、`Microsoft.Extensions.DependencyInjection`、`Microsoft.Extensions.Logging` 与 Serilog 文件日志；使用原生控件和轻量自绘 `Canvas`，第一版不依赖第三方图表包。

## 1. 建立解决方案与领域模型

创建 `Balancer.sln`、`src/Balancer.Core`、`src/Balancer.Infrastructure`、`src/Balancer.Wpf`、`tests/Balancer.Core.Tests`。在 Core 中定义角度/不平衡量、试重、测量运行、振动向量、影响系数矩阵、校正建议和数据质量枚举。约定复数相位的 0° 为键相上升沿，沿旋转方向为正。Infrastructure 承担 JSON 存储、日志、告警和可替换采集连接。

验证：解决方案可还原并编译；新增单位测试验证 g·mm、半径、质量换算和角度归一化。

## 2. 先实现并测试双平面影响系数求解器

先在 `Balancer.Core.Tests/InfluenceCoefficientSolverTests.cs` 写失败测试：理想 2×2 复矩阵能恢复已知校正向量；标定产生的矩阵与仿真真值相符；病态/奇异矩阵、缺少试验和无效试重被拒绝。随后实现 `InfluenceCoefficientSolver`：由基线和两次单独试重构建列向量，计算条件数，求解 `U_c = -H^{-1}V_0`，输出质量、角度与诊断。

验证：所有 solver 测试通过；校正后残余向量在理想场景接近零。

## 3. 先实现并测试键相与 1X 分析器

先新增测试：规则键相可恢复 RPM；按整转同步检波/DFT 能恢复已知 1X 幅值和相位；漏脉冲、转速波动超阈值或样本不足产生质量警告。实现 `TachometerAnalyzer`、`SynchronousVibrationAnalyzer`，输入为带时间戳的两个振动通道与键相信号，输出每个测点的复振动与质量指标。

验证：测试覆盖正常、带噪声、缺脉冲和快速变速案例并通过。

## 4. 实现可控的三通道仿真信号源与预处理

定义 `ISignalSource` 与 `SimulationSignalSource`。输入真实两面不平衡向量、真实 2×2 影响系数、RPM 曲线、传感器噪声、谐波、键相抖动/漏脉冲，输出两路压电波形和一路键相信号。试重或校正配重叠加至相应平面的不平衡矢量。实现可配置的电压转工程单位、去直流、限幅及低通/带通预处理管线；原始数据与预处理数据均可用于诊断。

验证：端到端测试从仿真信号提取的两个 1X 向量与模型的理论响应一致；应用求解器结果后的残余低于配置阈值。

## 5. 连接/通信接口 Demo、异常边界与告警

定义异步采集接口的连接状态、连接参数、数据帧和错误模型；实现 `SimulationSignalSource` 适配器及 TCP 行协议 Demo 适配器。TCP Demo 使用 UTF-8、以换行结束的一行 JSON：`timestampUtc`、`piezoA`、`piezoB`、`tach`；可配置主机/IP、端口、连接/读取超时。实现统一异常转换、告警服务（信息/警告/故障）和 Serilog 文件日志；连接失败、读超时、格式错误、字段缺失、掉线均记录并提示。

验证：本地回环服务器测试连接、正常帧、超时、断开和无效 JSON；测试告警级别、日志事件与 UI 可观察状态正确产生。

## 6. 标定会话、配方与设置

实现 `CalibrationSession`：只能按“基线 → 面 A 试重 → 面 B 试重”记录三次有效运行；保存试重质量、半径、角度、RPM 和采集质量；在创建矩阵前核验转速偏差、试重响应及条件数。实现 JSON 配方、标定和设置模型：配方保存转子、试重、预处理与分析阈值；全局设置保存界面、日志及连接参数，二者独立。

验证：测试不允许错误的试验顺序、未移除前一试重、超 RPM 偏差和不合格采集进入标定；正确记录可序列化并恢复。

## 7. 建立 WPF MVVM 操作界面与数据绑定

实现 `MainWindow`、`MainViewModel` 与服务注入。界面分为：连接参数与状态、配方选择/编辑、仿真参数、三步标定向导、两通道实时摘要、两面配重结果、质量/告警区和日志摘要。通过 MVVM 数据绑定更新实时值、命令可用性、连接状态和可确认告警。用 Canvas 显示两个振动矢量和建议配重相对键相的极坐标图；按钮包括连接、开始采集、记录当前步骤、计算校正、应用模拟校正、保存配方、导出结果。

验证：能从默认场景完成完整三步标定并显示两面建议；无效状态的按钮被禁用且告警可读。

## 8. 集成验证与交付文档

添加默认演示场景、端到端自动化测试，执行 `dotnet test` 与 `dotnet build`。在 `README.md` 说明：仿真工作流、角度约定、试重顺序、影响系数公式、数据质量限制、主要代码逻辑、配方和设置的存储位置，以及 TCP/采集接口 Demo 的连接参数、行 JSON 数据格式、超时/断线/异常处理方式。未来接入真实硬件仅需实现 `ISignalSource`。

验证：全量测试通过，WPF 项目编译成功，默认演示可复现校正前后残余下降。
