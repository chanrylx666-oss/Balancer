# WPF 动平衡测量仿真程序：初步设计

## 1. 目标与边界

构建一个基于 .NET 10 的双平面转子动平衡分析程序。当前没有硬件，因此程序由可控的仿真数据源生成压电振动信号和红外键相信号；分析链路应与未来的真实采集链路解耦，以便替换数据源后继续使用。程序同时提供常见通信/采集接口的 Demo 实现，供后续与实物联调。

一期聚焦于稳定转速下的双平面影响系数标定与校正：展示振动波形、转速、两测点的 1X 振动幅值/相位，并计算两个校正面的配重建议。不覆盖多于两平面的校正、硬件实时采集、设备保护联锁与正式计量认证。

## 2. 关键物理约束

压电传感器的振动幅值不天然等于转子不平衡量（g·mm）：两者还受转子质量、支撑刚度/阻尼、转速、安装位置、传感器灵敏度和机械传递路径影响。实际系统需通过“试重法/影响系数标定”建立换算关系。

因此仿真器会以两个平面的真实不平衡量和角度为输入，生成两个测点的带噪声振动；分析器则输出两个振动矢量，并基于试重得到的影响系数矩阵计算两平面的校正配重。未完成有效标定时界面必须明确显示“相对值/不可用于实际校正”。

## 3. 数据与算法链路

红外传感器每转产生一个键相信号（带脉冲宽度、抖动和偶发漏脉冲）。相邻上升沿时间差得到瞬时 RPM，并以该上升沿作为 0° 参考。

压电信号模型由与转频同步的 1X 正弦分量、可选 2X/3X 分量、结构共振增益、随机噪声和直流偏置组成。处理时按键相切分整转，对每转重采样为固定角度点，丢弃非稳态转速段；再使用同步检波/DFT 提取 1X 的复振动矢量（幅值、相位）。多转平均同时给出波动与置信度。

## 3.1 双平面影响系数法

使用两个轴承/机架测点 A、B，以及两个校正面 A、B。每次测量提取两个测点的复振动向量：V = [V_A, V_B]^T。首先测量原始状态 V_0；随后仅在校正面 A 加已知试重 U_A^t，测量 V_A^t；移除试重后，仅在校正面 B 加已知试重 U_B^t，测量 V_B^t。三次测量必须在同一稳定转速、同一键相参考和相同测点安装条件下完成。

影响系数矩阵为：

    H[:, A] = (V_A^t - V_0) / U_A^t
    H[:, B] = (V_B^t - V_0) / U_B^t
    H = [[H_AA, H_AB], [H_BA, H_BB]]

对于待校正的原始振动，求解校正不平衡向量：U_c = -H^(-1) V_0。每个复向量的模为不平衡量（g·mm），辐角为相对键相的角度；给定校正半径 r，可得质量 m = |U_c| / r。程序还须计算矩阵条件数：条件数过高、试重响应过小或两次转速偏差过大时，拒绝/警告标定结果。

## 4. WPF 模块边界

* `DataSource`：`ISignalSource` 抽象；一期包括 `SimulationSignalSource` 和可演示连接/读写/超时的采集接口 Demo。未来可加 NI-DAQ、串口或网络实现。
* `Acquisition`：时间戳、缓存、采样率与三通道数据帧（两个压电测点、一个键相）；采集前处理包含电压转工程单位、去直流、可配置低通/带通、限幅和掉样监控。
* `Analysis`：键相检测、RPM、稳态判定、角度重采样、1X 提取、平均及影响系数计算；应独立于 WPF 且可单元测试。
* `Domain`：两个校正面的参数、试重记录、2×2 复数影响系数矩阵、测量结果、配重建议、单位换算。
* `Presentation`：MVVM；仪表、趋势/波形、极坐标矢量图、参数表和状态提示。
* `Persistence`：JSON 保存/加载仿真场景、标定及测量报告；一期不依赖数据库。
* `Configuration`：配方（转子、试重、采集、分析阈值）与应用设置（界面、日志、连接参数）分开持久化；配方切换不得改写全局设置。
* `Infrastructure`：统一日志、异常边界、告警服务及通信连接状态。任何后台异常须记录上下文，并以非阻塞方式提示操作员。

## 5. 一期界面与流程

左侧为仿真与试验参数：转速、两个校正面的真实不平衡量/角度、传感器噪声、采样率、测量时长、两个校正半径。中间实时显示两个压电波形及红外脉冲，右侧显示 RPM、两个测点的 1X 幅值/相位、稳定度和信号质量。

用户流程：设置仿真参数 → 基线测量 V_0 → 在面 A 加已知试重并测量 → 移除 A 试重、在面 B 加已知试重并测量 → 自动计算并审核影响系数矩阵 → 生成两面的校正质量/角度建议 → 可模拟应用校正并做验证测量 → 导出报告。每个步骤需明确试重是否已移除。所有角度需注明方向约定（默认从键相沿转子旋转方向增加）。

## 6. 必要参数与验收场景

必须配置：采样率、目标 RPM/加减速曲线、每转脉冲数、相位零点、两个测点的传感器灵敏度/噪声、两个校正面的试重质量/半径/角度、两个校正半径。仿真模型还须有可编辑的真实 2×2 影响系数矩阵，以便制造交叉耦合。建议默认模拟 10–60 Hz 转频时采样率至少 5 kHz，以保留波形及高次谐波。

验收至少包括：已知两个 1X 信号能恢复各自幅值和相位；改变 RPM 仍能从键相得到正确转速；噪声增加时置信度下降；未完成三次测量时不输出伪造的 g·mm；两面有效试重后，计算出的矩阵在容差内复现仿真真值；应用两面校正后残余振动显著下降；漏脉冲、快速变速、病态矩阵或试重响应不足时显示数据质量警告。

## 7. 建议技术选型

.NET 10 WPF、MVVM（CommunityToolkit.Mvvm）、`Microsoft.Extensions.DependencyInjection`、`Microsoft.Extensions.Logging`、Serilog 文件日志、`System.Text.Json` 持久化、xUnit 单元测试。分析库保持纯 C#，图表与采集均以接口注入，确保未来接入 NI-DAQ、PLC、Modbus TCP 或串口时不影响算法。

## 7.1 通信/采集接口 Demo 约定

接口定义应覆盖 `ConnectAsync`、`DisconnectAsync`、`StartAsync`、`StopAsync` 与帧接收事件。连接参数包括接口类型、主机/IP、端口或 COM 口、波特率、校验位、数据位、停止位、连接超时和读超时。界面按连接状态显示“未连接 / 连接中 / 已连接 / 故障”。

第一版提供回环式 TCP 行协议 Demo：UTF-8，每行一个 JSON 数据帧（以 `\n` 结束），字段为 `timestampUtc`、`piezoA`、`piezoB`、`tach`，其中振动为原始电压，键相为 0/1。Demo 对连接失败、非 JSON、字段缺失、超时和连接断开分别产生结构化日志和可确认告警。其职责仅为演示通讯边界与数据格式，不宣称可替代硬件厂商协议。

## 7.2 操作员可见性与可靠性

界面使用 MVVM 数据绑定，将实时值、连接状态、标定步骤、配方名、告警级别及命令可用性绑定到 ViewModel。告警分为信息、警告、故障：例如采样质量下降为警告，连接断开/算法不可求解为故障。日志至少记录启动关闭、配方加载保存、连接参数（脱敏）、连接生命周期、采集统计、标定输入输出、告警确认与未处理异常。

## 8. 仍需确认的产品决定

1. 一期是否明确只做单平面，还是从一开始预留双平面工作流？
2. 最终要输出“教学/演示级的相对振动矢量”，还是要做完整的试重标定与配重建议？
3. 目标转速范围、转子类型、传感器数量和未来可能的采集硬件是什么？这些决定采样率、抗混叠与接口设计。
